#include "binary_trees.h"
/**
* heap_extract - function that extracts the root node
* @root: a double pointer to the root node of heap
* Return: a value stored in the root node
*/

int heap_extract(heap_t **root)
{
	printf("%d", (*root)->n);
	return (0);
}
